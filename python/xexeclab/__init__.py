"""polars-execution-lab -- execution analytics over crypto tick data.

The Python analytics mirror the Rust engine expression-for-expression so both
compute identical benchmarks on the same replay (see the equivalence test).
"""

__version__ = "0.1.0"
